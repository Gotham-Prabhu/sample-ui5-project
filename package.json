{
	"name": "sap-ui5-sheetjs",
	"version": "0.2.0",
	"description": "Ui5 app working with Sheet JS",
	"private": true,
	"scripts": {
		"start": "ui5 serve --port 8080",
		"lint": "eslint webapp",
		"build": "ui5 build -a --clean-dest",
		"build-self-contained": "ui5 build self-contained -a --clean-dest",
		"serve-dist": "ws --compress -d dist"
	},
	"dependencies": {},
	"devDependencies": {
		"@ui5/cli": "^2.11.2",
		"ui5-middleware-simpleproxy": "^0.7.2",
		"ui5-middleware-livereload": "^0.5.4",
		"eslint": "^7.28.0",
		"local-web-server": "^5.0.0",
		"rimraf": "^3.0.2"
	},
	"ui5": {
		"dependencies": [
			"ui5-middleware-simpleproxy",
			"ui5-middleware-livereload"
		]
	}
}
